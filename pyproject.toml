[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "geno-dev"
version = "0.1.0"
description = "Developer workflow skills and local usage recording"
requires-python = ">=3.11"
license = "MIT"
authors = [{ name = "42euge" }]

[project.optional-dependencies]
test = ["pytest>=8.0"]

[project.scripts]
geno-dev = "geno_dev.cli:main"

[tool.setuptools.packages.find]
include = ["geno_dev*"]
