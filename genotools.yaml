name: geno-dev
version: 0.1.0
description: Developer utilities — task execution, commit rewriting, Colab config/upload

# No venv or scripts — all commands are pure markdown workflows.

config:
  - {src: config/defaults/colab.json, dst: colab.json}

commands:
  source: commands/
